== Modulecounts

Uses 'rake cron' to poll all known repos for data each night.



== Dev Setup

Runs on heroku bamboo stack with MRI 1.9.2

To run bundle install:

   PATH=$PATH:/Library/PostgreSQL/9.3/bin/ bundle

